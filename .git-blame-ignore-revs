# Commits that changed formatting and nothing else. git blame skips these, so an
# annotation points at whoever last changed the code rather than at the reformat.
#
# Turn it on once, per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file on its own and needs no configuration.

# style: format the repository with Biome
e95617462de15841d05a5f33fe95df73a1748ef5
